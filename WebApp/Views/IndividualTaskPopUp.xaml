<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage 
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
    xmlns:animations="clr-namespace:Rg.Plugins.Popup.Animations;assembly=Rg.Plugins.Popup"
    x:Class="WebApp.Views.IndividualTaskPopUp">
    <!--You can set an animation in the xaml file or in the csharp code behind-->
    <pages:PopupPage.Animation>
        <animations:ScaleAnimation 
            PositionIn="Center"
            PositionOut="Center"
            ScaleIn="1.2"
            ScaleOut="0.8"
            DurationIn="400"
            DurationOut="300"
            EasingIn="SinOut"
            EasingOut="SinIn"
            HasBackgroundAnimation="True"/>
    </pages:PopupPage.Animation>
    <!--You can use any elements here which are extended from Xamarin.Forms.View-->
    <StackLayout 
        VerticalOptions="Center" 
        HorizontalOptions="Center" 
        Padding="20, 20, 20, 20">
        <Label x:Name="TaskName" FontSize="50" TextColor="Yellow" HorizontalOptions="Center"/>
        <Grid HorizontalOptions="Center" Padding="20,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Text="Deadline: " Grid.Column="0" Grid.Row="1"/>
            <Label x:Name="Deadline" Grid.Column="1" Grid.Row="1"/>
            <Label Text="Task id: " Grid.Column="0" Grid.Row="2"/>
            <Label x:Name="TaskId" Grid.Column="1" Grid.Row="2"/>
            <Label Text="Penalty: " Grid.Column="0" Grid.Row="3"/>
            <Label x:Name="Penalty" Grid.Column="1" Grid.Row="3"/>
        </Grid>
        <Label Text="Members :" FontSize="40" TextColor="White" HorizontalOptions="Center"/>
        <ScrollView Orientation="Vertical" HeightRequest="100" Padding="30, 0">
            <StackLayout Orientation="Vertical" x:Name="FriendsProgress" />
        </ScrollView>
        <Button x:Name="sendingButton" Text="TAKE PICTURE" FontSize="40" 
                HorizontalOptions="Center" Clicked="OnClicked"/>
    </StackLayout>
</pages:PopupPage>